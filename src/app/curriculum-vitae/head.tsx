import type { ReactElement } from "react";

const CurriculumVitaeHead = (): ReactElement => {
  return (
    <style>{`
      html, body {
        -webkit-text-size-adjust: none;
        text-size-adjust: none;
      }
    `}</style>
  );
};

export default CurriculumVitaeHead;
